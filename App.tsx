import React from 'react';
import {
  SafeAreaView,
  Text,
} from 'react-native';
import Form from './src/components/Form';

function App(): React.JSX.Element {

  return (
    <SafeAreaView>
      <Form/>
    </SafeAreaView>
  );
}

export default App;
